// In your project, create a store directory (e.g., client/src/store)

// store/index.js
import { configureStore } from '@reduxjs/toolkit';
import rootReducer from './reducers';

// const store = configureStore(rootReducer);

// export default store;
